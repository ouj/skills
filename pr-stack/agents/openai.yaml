interface:
  display_name: "PR Stack"
  short_description: "Manage stacked PRs and retarget their base branches"
  default_prompt: "Use $pr-stack to inspect, restack, retarget, and submit a stack of dependent GitHub pull requests so each PR tracks its recorded parent branch without Graphite."
