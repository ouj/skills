interface:
  display_name: "PR Stack"
  short_description: "Manage stacked PR branches without Graphite"
  default_prompt: "Use $pr-stack to inspect, restack, and submit a stack of dependent GitHub pull requests without Graphite."
